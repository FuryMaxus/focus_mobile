package com.example.focus.ui.theme

import androidx.compose.ui.graphics.Color

// ═══════════════════════════════════════════════════════════════
//  FOCUS MOBILE — Design System · Color Tokens
//  Estética: D&D Guild Hall × Don't Starve ink
//  Fuente visual: Visual Identity Kit (image_2.png)
// ═══════════════════════════════════════════════════════════════

// ── Fondos ─────────────────────────────────────────────────────
/** Fondo principal. Negro de mazmorra, casi sin tono. */
val DungeonNoir       = Color(0xFF1A1A1D)

/** Superficie de tarjetas / capas elevadas sobre el fondo. */
val DungeonNoir700    = Color(0xFF232328)

/** Superficie más clara para diálogos o bottom sheets. */
val DungeonNoir500    = Color(0xFF2E2E35)

// ── Cuero y madera ─────────────────────────────────────────────
/** Tarjetas de inventario, bordes de artefactos. */
val SaddleBrown       = Color(0xFF8B4513)

/** Versión más clara para estados hover / pressed. */
val SaddleBrown300    = Color(0xFFAD5A1E)

/** Sombra profunda de cuero envejecido. */
val SaddleBrown700    = Color(0xFF5C2C0A)

// ── Oro y llama ────────────────────────────────────────────────
/** Títulos, íconos premium, bordes activos. Oro antiguo. */
val AncientGold       = Color(0xFFD4AF37)

/** Variante clara: glow de oro en estados focus. */
val AncientGold200    = Color(0xFFEDD97A)

/** Variante oscura: oro apagado para textos secundarios dorados. */
val AncientGold700    = Color(0xFF9A7D1E)

/** Botón primario de acción. Llama de ámbar. */
val AmberFlame        = Color(0xFFE67E22)

/** Pressed/hover del botón primario. */
val AmberFlame700     = Color(0xFFBF6010)

/** Brillo del botón en estado ripple. */
val AmberFlame200     = Color(0xFFF0A35E)

// ── Texto y bordes ─────────────────────────────────────────────
/** Texto principal sobre fondo oscuro. Plata de acero. */
val SteelSilver       = Color(0xFFC0C0C0)

/** Texto secundario / placeholders. */
val SteelSilver500    = Color(0xFF8A8A8A)

/** Bordes sutiles de inputs inactivos. */
val SteelSilver200    = Color(0xFF4A4A4A)

// ── Alertas y estado ───────────────────────────────────────────
/** Error / alerta crítica. Rojo dragón, casi negro. */
val DragonRed         = Color(0xFF8B0000)

/** Fondo de snackbar de error. */
val DragonRedSurface  = Color(0xFF3D1C02)

/** Éxito / misión completada. Verde musgo de dungeon. */
val DungeonGreen      = Color(0xFF4A7C59)

/** Información / rareza mágica. Violeta runa. */
val RuneViolet        = Color(0xFF6A4C9C)

// ── Utilidades ─────────────────────────────────────────────────
/** Blanco puro para íconos sobre fondos oscuros de botón. */
val PureWhite         = Color(0xFFFFFFFF)

/** Negro de tinta para outlines estilo Don't Starve. */
val InkBlack          = Color(0xFF0D0D0F)

/** Scrim / overlay para modales. */
val Scrim             = Color(0xCC0D0D0F)